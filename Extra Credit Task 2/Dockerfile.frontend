FROM python:3.13-slim

WORKDIR /app

RUN pip install --no-cache-dir streamlit requests

COPY app.py /app/app.py

ENV BASE_API_URL=http://langflow-backend:7860

EXPOSE 8501

CMD ["streamlit", "run", "app.py", "--server.address=0.0.0.0", "--server.port=8501"]
